package com.github.sanmoo.messages.handlers

import com.github.sanmoo.messages.UpdateResourceADownstreamCommand

class UpdateResourceADownstreamCommandHandler {
    fun process(message: UpdateResourceADownstreamCommand) {
        // inserts or modify data in downstream system, for example by writing to its database
    }
}