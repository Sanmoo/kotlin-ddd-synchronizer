package com.github.sanmoo.messages.handlers

import com.github.sanmoo.messages.CreateResourceADownstreamCommand

class CreateResourceADownstreamCommandHandler {
    fun process(message: CreateResourceADownstreamCommand) {
        // inserts or modify data in downstream system, for example by writing to its database
    }
}